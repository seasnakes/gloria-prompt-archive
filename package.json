{
  "name": "gloria-prompt-archive",
  "version": "1.0.2",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 5182 --strictPort",
    "build": "node scripts/validate.mjs && vite build",
    "preview": "vite preview --host 127.0.0.1 --port 5183 --strictPort",
    "test": "node --test tests/*.test.mjs",
    "sync": "python3 scripts/sync_feishu.py",
    "publish": "python3 scripts/publish.py"
  },
  "dependencies": {
    "three": "0.180.0",
    "typed.js": "2.0.12"
  },
  "devDependencies": {
    "vite": "7.1.4"
  }
}
